// They think of something you could store in a TypeScript Object. 
// Write a program that creates Objects containing these items.
 let typeobj={

    name:"ali",
    age:20,
    address:"Lahore"
 }
 console.log(`The name of person is ${typeobj.name}`)
 console.log(`The age of person is ${typeobj.age}`)
 console.log(`The address of person is ${typeobj.name}`)
 export{}